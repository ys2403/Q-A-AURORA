# Q-A-AURORA
AURORA_TEST is a FastAPI-based REST API that provides intelligent responses to user queries using LangChain, FAISS vector search, and Hugging Face embeddings. It’s designed for lightweight deployment (Render) and demonstrates a complete Retrieval-Augmented Generation (RAG) workflow in production-ready form.
